const BreathingIndicator = () => (
  <div className="flex items-center gap-3 py-2 px-1">
    <div className="w-3 h-3 rounded-full bg-sanctuary-sage breathing" />
    <span className="text-sanctuary-muted text-sm font-body">AILYN está presente...</span>
  </div>
);

export default BreathingIndicator;
